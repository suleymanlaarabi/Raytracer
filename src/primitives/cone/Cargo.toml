[package]
name = "raytracer_cone"
version = "0.1.0"
edition = "2024"

[lib]
name = "raytracer_cone"
crate-type = ["cdylib"]

[dependencies]
ron = "0.12"
serde = { version = "1", features = ["derive"] }
raytracer = { path = "../../raytracer", default-features = false }
