[package]
name = "raytracer_plane"
version = "0.1.0"
edition = "2024"

[lib]
name = "raytracer_plane"
crate-type = ["cdylib"]

[dependencies]
ron = "0.12"
serde = { version = "1", features = ["derive"] }
raytracer = { path = "../../raytracer", default-features = false }
